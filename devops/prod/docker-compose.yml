services:
  company-profile:
    build:
      context: ../..
      dockerfile: devops/Dockerfile
      args:
        - DEPLOYMENT_ID=${DEPLOYMENT_ID}
        - NODE_ENV=${NODE_ENV}
        - CACHEBUST=${CACHEBUST}
    container_name: company-profile-router
    restart: always
    environment:
      - NODE_ENV=${NODE_ENV}
      - NEXT_PUBLIC_API_BASE_URL=${BE_URL}
      - NEXTAUTH_SECRET=${NEXTAUTH_SECRET}
      - NEXT_PUBLIC_API_PATH=api
      - NEXT_PUBLIC_API_VERSION=v1
      - NEXT_PUBLIC_CACHE_BUST=${CACHEBUST}
      - DEPLOYMENT_ID=${DEPLOYMENT_ID}
    labels:
      - "traefik.enable=true"
      # Option 1: Use a different domain
      - "traefik.http.routers.company-profile-router.rule=HostRegexp(`^website(-[a-z0-9-]+)?\\.constrix-nv\\.com$`) || PathPrefix(`/`)"
      # Option 2: Use a path prefix (uncomment and comment line above)
      # - "traefik.http.routers.company-profile-router.rule=PathPrefix(`/company`)"
      - "traefik.http.routers.company-profile-router.entrypoints=https"
      - "traefik.http.routers.company-profile-router.tls.certresolver=letsencrypt"
      - "traefik.http.services.company-profile-router.loadbalancer.server.port=3001"
      - "traefik.http.routers.company-profile-router.priority=9"
      # HTTP router - redirects to HTTPS
      - "traefik.http.routers.company-profile-router-http.rule=HostRegexp(`^website(-[a-z0-9-]+)?\\.constrix-nv\\.com$`) || PathPrefix(`/`)"
      - "traefik.http.routers.company-profile-router-http.entrypoints=http"
      - "traefik.http.routers.company-profile-router-http.priority=9"
      - "traefik.http.routers.company-profile-router-http.middlewares=redirect-to-https"
      - "traefik.http.middlewares.redirect-to-https.redirectscheme.scheme=https"
      - "traefik.http.middlewares.redirect-to-https.redirectscheme.permanent=true"
    networks:
      - traefik_network

networks:
  traefik_network:
    external: true

