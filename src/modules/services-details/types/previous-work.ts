/**
 * Previous work item type definition
 * Used in service details to showcase completed projects
 */
export interface PreviousWork {
  id: number;
  title: string;
  description: string;
  image: string;
}

